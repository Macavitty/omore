import 'package:flutter/material.dart';

class IoTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('хаха, наивная'),
      ),
    );
  }
}